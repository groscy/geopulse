# add-weather-temp-labels

Numeric per-country temperature labels on the globe's Meteorological overlay, with a size-based level-of-detail rule so smaller countries reveal their temperature only when zoomed in. Frontend-only; builds on the archived `add-weather-domain`.
