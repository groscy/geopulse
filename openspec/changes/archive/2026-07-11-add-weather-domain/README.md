# add-weather-domain

Real per-country weekly-average temperature folded into the existing Meteorological overlay, scored as a standalone `weather` anomaly domain (self-relative z), with heat/cold-anomaly incidents. Keyless (Open-Meteo), no DB migration. Phase 1 of a weather roadmap whose later phases (3D atmospheric field shell, live storm tracks) are captured in design.md.
